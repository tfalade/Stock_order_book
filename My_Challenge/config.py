"""Configuration variables for project.
"""

import os

# ROOT_DIR is required to specify the root directory, in order to read/write files correctly for PyTest. This file location is important because of the __file__ variable.
ROOT_DIR = os.path.dirname(os.path.abspath(__file__))